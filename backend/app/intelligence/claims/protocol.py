"""Claim Intelligence protocol package path.

The historical protocol module remains the implementation authority during
the no-behavior-change architecture migration because its source surface is
contract-tested.
"""

from app.intelligence.claim_semantic_protocol_ownership import *
from app.intelligence.claim_semantic_protocol_ownership import __all__